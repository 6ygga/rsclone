export default class MultiplicationTableController {
  #model;

  #view;

  constructor(model, view) {
    this.#model = model;
    this.#view = view;

    // this.#view.emitter.on('', () => this.method());
  }

  // method() {
  //   this.#model.method();
  // }
}
