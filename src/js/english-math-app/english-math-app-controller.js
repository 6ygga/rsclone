export default class EnglishMathAppController {
  #model;

  #view;

  constructor(model, view) {
    this.#model = model;
    this.#view = view;
  }

  changePage() {
    // this.#model.changeRoute();
  }
}
